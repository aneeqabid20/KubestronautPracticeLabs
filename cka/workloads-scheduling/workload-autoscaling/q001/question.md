# Question

HPA `web` in namespace `cka-ws-q005` has incorrect scaling parameters.

Configure it as follows:
- minimum replicas: `2`
- maximum replicas: `6`
- CPU utilization target: `60%`

Keep the target Deployment unchanged.

**Target time:** 5 minutes
