# Question

Pod `worker` in namespace `cka-tr-q007` repeatedly restarts.

Use the container output to determine the required application setting and restore
the Pod.

## Requirements

- Keep the Pod name `worker`.
- Set environment variable `MODE=production`.
- Pod must become Ready.
- Do not change the image.

**Target time:** 5 minutes
