# Question

A Kustomize project has been copied to `/tmp/cka-ca-q011`.

Correct the `prod` overlay and apply it.

## Requirements

- Work only in `/tmp/cka-ca-q011`.
- Deployment `web` must have 3 replicas.
- Image must be `nginx:1.27-alpine`.
- Apply using Kustomize (`kubectl apply -k` is acceptable).
- All replicas must be Ready.

**Target time:** 8 minutes
