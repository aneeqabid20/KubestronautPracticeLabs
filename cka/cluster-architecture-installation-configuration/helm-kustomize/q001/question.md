# Question

Install the local Helm chart in `resources/chart` as release `web` in namespace
`cka-ca-q010`.

## Requirements

- Release name: `web`.
- Set `replicaCount=3`.
- Set Service port to `8080`.
- Deployment must have 3 Ready replicas.
- Do not modify the chart templates.

**Target time:** 8 minutes
